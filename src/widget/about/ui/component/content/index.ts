"use client";
import { createGroupSlideAlternating } from "@/shared/lib/animations/factory/group.factory";
import { ContentAbout } from "./content.about";

export const ContentAboutAnimated = createGroupSlideAlternating(ContentAbout);
