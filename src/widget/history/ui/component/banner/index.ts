"use client";

import { createContainerGroupItemRef } from "@/shared/lib/animations";
import { BannerHistory } from "./banner.history";

export const BannerHistoryAnimated = createContainerGroupItemRef(BannerHistory);
