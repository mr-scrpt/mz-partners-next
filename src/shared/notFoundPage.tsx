import PageLayout from "./ui/layout/ui/page.layout";

export default function NotFoundPage() {
  return (
    <PageLayout title={"NOT FOUND CUSTOM"}>
      <p className="max-w-[460px]">NOT FOUND CUSTOM</p>
    </PageLayout>
  );
}
